
> [!attention] 
> $$p(y\ |\ X,w) = \prod_i p_i^{y_i}(1-p_i)^{1-y_i}$$ 
> Оптимизировать такое произведение неудобно и тяжело, поэтому мы перейдём к логарифмическому правдоподобию: $$\begin{multline*}\ell (w,X,y)=\sum_i(y_i\log(p_i)+(1-y_i)\log(1-p_i)) =\\ \sum_i(y_i\log(\sigma(\langle w,x_i \rangle))+(1-y_i)\log(1-\sigma(\langle w,x_i \rangle)))\end{multline*}$$

> [!info] 
> $$\sigma(-z)=1-\sigma(z)$$

> [!done] 
> $$\ell(w,X,y)=\sum_i(y_i\log(\sigma(\langle w,x_i \rangle))+(1-y_i)\log(\sigma(-\langle w,x_i \rangle)))$$

