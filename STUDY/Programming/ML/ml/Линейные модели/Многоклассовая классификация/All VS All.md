
> [!idea] 
> Обучим $C^2_k$ классификаторов $a_{ij},i,j=1,...,K,i\neq j.$ Так, например, $$b_{ij}(x)=sgn(\langle w_{ij},x \rangle + w_{0,ij})$$
> Классификатор $a_{ij}(x)$ будем настраивать по подвыборке $X_{ij}\subset X$, содержащей только объекты классов $i\ и\ j$. Получается, что для любого объекта классификатор $a_{ij}$ будет выдавать принадлежность либо к классу $i$, либо к классу $j$.

![[Pasted image 20251223120720.png]]

> [!key] 
> Чтобы классифицировать новый объект, подадим его на вход каждого из построенных бинарных классификаторов. Каждый из них проголосует за свой класс; в качестве ответа выберем тот класс, за который наберется больше всего голосов: $$a(x)=argmax_k\sum_{i=1}^K\sum_{j\neq i}\mathbb{I}[a_{ij}(x)=k]$$
> 

![[Pasted image 20251223120843.png]]

> [!problem] 
> Обратите внимание на серый треугольник на стыке областей. Это точки, для которых голоса разделились (в данном случае каждый классификатор выдал какой-то свой класс, то есть у каждого класса было по одному голосу). Для этих точек нет явного способа выдать обоснованное предсказание.



