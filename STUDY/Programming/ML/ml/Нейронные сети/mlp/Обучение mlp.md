
> [!goal] 
> Мы хотим подобрать `параметры перцептрона` так, чтобы максимизировать произведение вероятностей правильной классификации по всем элементам обучающей выборки.

> [!task] 
> Пусть $(x,y)$ - элемент обучающей выборки, $\theta$ - вектор параметров нейросети.
> На последнем слое нейронной сети - вероятности классов $$p = (p_0...p_k)^T$$

> [!solved] 
> Оптимизируем $logloss(y,p):$ $$-\log p_y(x)\rightarrow\min$$

> [!finally] 
> Итоговая задача оптимизации: $$\frac{1}{\mathcal{l}}\sum_{(x,y)\in X_{train}}logloss(y,p(x,\theta))\rightarrow\min_{\theta}$$


