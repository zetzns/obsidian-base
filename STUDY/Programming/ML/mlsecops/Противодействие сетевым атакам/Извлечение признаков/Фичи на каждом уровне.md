> [!list] 
> - `Уровень сессии`: packet count, byte count, duration...
> - `Уровень приложений`: HTTP method/URL/Host, DNS query/name/type, TLS SNI/cert info, payload size, application headers
> - `Транспортный уровень`: src/dst ports, TCP flags, seq/ack, window
> - `Межсетевой уровень`: src/dst IP, TTL, IP flags, packet length, fragmentation info
