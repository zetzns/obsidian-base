
> [!important] 
> `Embedding models` — это нейронные сети, специально обученные понимать семантические отношения:

```python
# Упрощённый процесс обучения
# 1. Модель видит миллионы текстовых пар
# 2. Учится тому, что похожие тексты должны иметь похожие эмбеддинги
# 3. Учится тому, что непохожие тексты должны иметь разные эмбеддинги

training_pairs = [
    ("dog", "canine", "similar"),
    ("dog", "car", "different"),
    ("energy production", "powerhouse", "similar"),
    ("cell organelle", "mitochondria", "similar")
]

# После обучения модель может осмысленно встраивать ЛЮБОЙ текст

```
