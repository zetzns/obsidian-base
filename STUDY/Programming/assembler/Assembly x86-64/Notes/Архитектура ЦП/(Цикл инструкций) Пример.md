> [!example]
> 1. Извлечь инструкцию из `rip`регистра `48 83 C0 01`(в двоичном формате).
> 2. Расшифруйте ' `48 83 C0 01`', чтобы узнать, что необходимо выполнить операцию `add`of `1`для значения at `rax`.
> 3. Получить текущее значение `rax`(по `CU`), добавить `1`к нему (по `ALU`).
> 4. Сохраните новое значение обратно в `rax`.

