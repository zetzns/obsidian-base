
> [!finally] 
> `Линковщик` собирает объектные файлы и создаёт итоговый исполняемый файл.

> [!fact] 
> Линковщик подключает все нужные библиотеки (libc в т.ч.) и создаёт `динамическое` или `статическое` подключение к таковым для файла.



